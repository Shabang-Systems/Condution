/**
 * A collection of Widgets
 *
 * A view has a name (duh) and some widgets in order
 * to tell @FlynNick and @TheEnquirer what to render 
 * in the actual app. It is similar to a "datapack"
 * that bunches a group of things together.
 *
 * The group also hooks onto refresh hooks, which allows
 * React to know when to refresh the view.
 *
 */

class View {
    // TODO
}


export { View };

